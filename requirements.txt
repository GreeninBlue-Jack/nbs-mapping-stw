geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pandas>=2.0
numpy>=1.24
requests>=2.31
pyyaml>=6.0
matplotlib>=3.7
folium>=0.15
jupyter>=1.0
fiona>=1.9
pyogrio>=0.7
pyproj>=3.6
owslib>=0.31
scipy>=1.11
urllib3>=2.0

# NOTE: these are loose declarations. The geopandas / shapely / pyogrio / fiona / pyproj
# stack has ABI coupling and has broken across minor releases. If anything misbehaves,
# install the pinned known-good set instead:  pip install -r requirements-lock.txt
# (scripts/qgis_review_comparison.py and scripts/style_zoomstack.py additionally need the
#  QGIS Python environment — `qgis` is not pip-installable. They are dev/optional only.)
