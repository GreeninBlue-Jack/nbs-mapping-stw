# Virtual environment
venv/
.venv/
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/

# Data - too large for GitHub, also some entries (e.g. OS PSGA MasterMap) are
# licensed restricted-distribution. Keep the README so the convention is tracked.
data/raw/*
!data/raw/README.md
data/processed/

# Outputs - regenerated by running the pipeline.
# Use outputs/* + negation so we can selectively track audit history below.
outputs/*
!outputs/data_audit/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.claude/
*.swp

# Original R model reference (FutureWater / N4W co-authored IP; contains OS-derived
# shapefiles + large GeoTIFFs) — not shipped in the handover repo (Brief 24 Task 6).
data/reference/
.Rproj.user/
*.Rhistory

# Draft deliverable docs — delivered to the client separately, not in the repo (Task 6).
docs/STW_NbS_Opportunity_Mapping_Methodology_DRAFT.*
